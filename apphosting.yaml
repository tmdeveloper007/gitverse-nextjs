# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
env:
  - variable: NEXT_PUBLIC_FIREBASE_API_KEY
    secret: webapp-firebase-api-key
  - variable: GEMINI_API_KEY
    secret: gemini-api-key
  - variable: DATABASE_URL
    secret: database-url
  - variable: JWT_SECRET
    secret: jwt-secret

  # NextAuth (Google OAuth)
  # NOTE: NEXTAUTH_URL must match your deployed origin (e.g. https://<your-domain>).
  - variable: NEXTAUTH_URL
    secret: nextauth-url
  - variable: NEXTAUTH_SECRET
    secret: nextauth-secret
  - variable: GOOGLE_CLIENT_ID
    secret: google-client-id
  - variable: GOOGLE_CLIENT_SECRET
    secret: google-client-secret

  # Worker auth secrets
  - variable: INTERNAL_WORKER_SECRET
    secret: internal-worker-secret
  - variable: ANALYSIS_RUNNER_SECRET
    secret: analysis-runner-secret
  - variable: TOKEN_ENCRYPTION_KEY
    secret: token-encryption-key

  # Prisma / Postgres tuning (safe defaults for Cloud Run)
  - variable: PRISMA_ADAPTER
    value: pg
  - variable: PG_POOL_MAX
    value: "2"
  - variable: PG_POOL_CONNECTION_TIMEOUT_MS
    value: "30000"
