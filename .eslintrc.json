{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-console": ["warn", { "allow": ["warn", "error"] }]
  },
  "overrides": [
    {
      "files": ["scripts/**", "*.config.*", "*.config.js", "*.config.ts", "*.config.mjs", "jest.config.*", "next.config.*", "playwright.config.*"],
      "rules": {
        "no-console": "off"
      }
    }
  ]
}